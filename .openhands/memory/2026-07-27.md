# 2026-07-27 — automation health PR (#325)

- Issue #323 "auto-disable unhealthy automations". Minimal PR #325 (draft) on branch `feat/minimal-automation-health`, vs the larger competing PR #324.
- Design: one recorder `apply_terminal_health` (utils/run.py) + `health.py` classifier reusing SDK `openhands.sdk.event.error_classification.FailureKind/classify_error`. Counts AUTH/CONFIG/QUOTA + COMPLETED-with-blocking_reason; resets on RATE_LIMIT/TRANSIENT/success. Disable at `AUTOMATION_UNHEALTHY_FAILURE_THRESHOLD` (default 3), mark queued PENDING->SKIPPED, re-enable clears streak. Migration 015.
- 4 DB columns only: automations.consecutive_unhealthy_runs, automations.disabled_reason, runs.failure_kind, runs.blocking_reason. (Skipped `disabled_failure_kind` as redundant.)
- SDK 1.40.1 workspace callback does NOT yet send failure_kind/blocking_reason (only status/error/conversation_id/cost) — agent-attached blocking factor needs a tiny SDK follow-up.
- Key gotcha: ruff pre-commit hook `--fix --exit-non-zero-on-fix` can auto-add isort blank lines that a prior `pre-commit run --all-files` missed (test file drift). Always re-check `git diff` after pre-commit and re-run the exact hook on new files before pushing.
- SDK classify_error: "connection timed out" -> UNKNOWN (token list has "connection error"/"timeout" but not "timed out"); ValueError class name is NOT mapped by SDK, so health.classify_exception maps (PermanentDispatchError, ValueError)->CONFIG explicitly.
- CI: tests.yml runs full pytest vs Postgres testcontainer (Docker needed locally); ci.yml runs pre-commit all-files; pr-title lint enforces conventional `feat:` prefix.

# SDK release prep (software-agent-sdk)
- SDK PR #4458 (draft, branch feat/report-automation-blocking-factor): BaseWorkspace completes the callback contract needed by automation #325.
  - `register_blocking_factor(kind, reason)` added to `openhands-sdk/openhands/sdk/workspace/base.py`; `_send_completion_callback` now emits `failure_kind` (auto-classified via classify_error on exception) + `blocking_reason`. Tests in tests/sdk/workspace/remote/test_remote_workspace.py. All CI green.
- SDK release path (README-RELEASE.md): merge feat PR -> Prepare Release workflow (manual, gives X.Y.Z) -> approve release PR -> create GitHub release (tag vX.Y.Z) -> pypi-release.yml publishes -> version-bump-prs.yml auto-opens `bump-sdk-X.Y.Z` PR in OpenHands/automation (pins openhands-sdk + openhands-workspace). That bump is how automation #325 incorporates this.
- automation presets use openhands.sdk.workspace.remote.RemoteWorkspace (openhands-sdk pkg), NOT openhands.workspace.OpenHandsCloudWorkspace; the latter is a separate package and off the preset callback path.
- SDK monorepo is a uv workspace with members openhands-sdk/tools/workspace/agent-server; pre-commit hooks include Check import dependency rules + Check Tool subclass registration.
